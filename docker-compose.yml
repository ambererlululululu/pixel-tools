services:
  pixel-tools:
    build: .
    ports:
      - "3001:3000"
    env_file:
      - .env.production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 1G
